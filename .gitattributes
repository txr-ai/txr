cd ~/txr-verify

cat > .gitattributes <<'EOF'
# Keep LF for text by default (no CRLF flips)
* text=auto eol=lf

# Treat hashed artifacts as binary (no line-ending conversion)
*.jsonl -text
*.csv  -text
*.xml  -text
*.sig  -text
SHA256SUMS.txt -text
*.zip  -text
*.png  -text
EOF
